# PostgreSQL credentials for the Laravel app.
# These must match the app's DB_USERNAME / DB_PASSWORD / DB_DATABASE.
# CHANGE THE PASSWORD before any real deployment.
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password
POSTGRES_DB=test
