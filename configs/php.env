# PHP / runtime tuning (from compose x-php-environment).
# php.service uses AUTORUN_ENABLED=true; workers override it to false in their unit.
PUID=1000
PGID=1000
AUTORUN_ENABLED=true
AUTORUN_LARAVEL_MIGRATION=true
AUTORUN_LARAVEL_MIGRATION_FORCE=true
# Migration isolation is OFF on purpose. `migrate --isolated` takes its lock via
# the DEFAULT cache store, which is CACHE_STORE=database (see laravel.env). On a
# fresh DB the `cache_locks` table doesn't exist yet (it's created *by* the
# migrations), so isolation deadlocks the very first migrate and the container
# crash-loops. Only php.service runs migrations (the queue/scheduler units set
# AUTORUN_ENABLED=false), so there's no concurrent migrator to guard against.
AUTORUN_LARAVEL_MIGRATION_ISOLATION=false
# Caching/optimization default to true; set explicitly to document prod intent.
AUTORUN_LARAVEL_OPTIMIZE=true
AUTORUN_LARAVEL_CONFIG_CACHE=true
AUTORUN_LARAVEL_ROUTE_CACHE=true
AUTORUN_LARAVEL_VIEW_CACHE=true
AUTORUN_LARAVEL_EVENT_CACHE=true
AUTORUN_LARAVEL_STORAGE_LINK=true
PHP_OPCACHE_ENABLE=1
PHP_OPCACHE_MEMORY_CONSUMPTION=256
PHP_OPCACHE_INTERNED_STRINGS_BUFFER=16
PHP_OPCACHE_MAX_ACCELERATED_FILES=10000
PHP_OPCACHE_VALIDATE_TIMESTAMPS=0
PHP_POST_MAX_SIZE=120M
PHP_UPLOAD_MAX_FILE_SIZE=120M
PHP_MAX_FILE_UPLOADS=100
